name: aqlinter
description: "This is AQoong's favorit linter set"
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

topics:
  - lints